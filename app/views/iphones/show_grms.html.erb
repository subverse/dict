<ul id = "show_grm" title=<%= "#{@grammar.grm}" %> selected="true">
	<% @vocs.each do |voc| %>
		<li>
			<%= link_to "#{voc.german}",
				:controller => :iphones, 
				:action => :show_voc, 
				:id => voc.id %>
			[<%= voc.wylie %>]
		</li>	
	<% end %> 		
</ul>	
